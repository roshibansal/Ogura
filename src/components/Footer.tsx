import { LuxuryFooter } from "@/components/LuxuryFooter";

// One footer across the whole site. This file used to hold a second, white
// footer with its own (different) links, so half the pages disagreed with the
// other half about what Ogura links to.
export const Footer = LuxuryFooter;
